# Frontend production config reminders

# Update these in Angular files before building/deploying:

# 1) API URL (zfunds-frontend/src/environments/environment.prod.ts)
# apiUrl: 'https://your-backend.railway.app/api'

# 2) reCAPTCHA site key (zfunds-frontend/src/app/services/recaptcha.service.ts)
# private siteKey: string = 'YOUR_PRODUCTION_SITE_KEY';

